-- SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local SoundService = game:GetService("SoundService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local root = character:WaitForChild("HumanoidRootPart")

-- 🔒 BLOQUER LES MOUVEMENTS
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0
humanoid.AutoRotate = false
root.Anchored = true

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RGB_HUDFREEZE"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

-- 🖼️ IMAGE DE FOND
local Background = Instance.new("ImageLabel")
Background.Parent = ScreenGui
Background.Size = UDim2.new(1, 0, 1, 0)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.Image = "rbxassetid://1234567890"
Background.BackgroundTransparency = 1
Background.ImageTransparency = 0.3
Background.ScaleType = Enum.ScaleType.Stretch
Background.ZIndex = 0

-- 🎵 MUSIQUE
local Sound = Instance.new("Sound")
Sound.Parent = SoundService
Sound.SoundId = "rbxassetid://137496465733794"
Sound.Volume = 999 -- 2000 est beaucoup trop élevé
Sound.Looped = true
Sound:Play()

-- HUDS
local huds = {}

for i = 1, 25 do
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Parent = ScreenGui
    TextLabel.Size = UDim2.new(0, 200, 0, 60)
    TextLabel.Position = UDim2.new(math.random(), -100, math.random(), -30)
    TextLabel.Text = "Hacked by Skowzk and Nemosito 😂"
    TextLabel.TextScaled = true
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.BackgroundTransparency = 0.2
    TextLabel.BorderSizePixel = 0
    TextLabel.ZIndex = 1

    table.insert(huds, TextLabel)
end

-- 🌈 RGB ANIMATION
local hue = 0
RunService.RenderStepped:Connect(function()
    hue = (hue + 0.01) % 1
    local bgColor = Color3.fromHSV(hue, 1, 1)
    local textColor = Color3.fromHSV((hue + 0.5) % 1, 1, 1)

    for _, hud in pairs(huds) do -- ✅ correction ici
        hud.BackgroundColor3 = bgColor
        hud.TextColor3 = textColor
    end
end)

